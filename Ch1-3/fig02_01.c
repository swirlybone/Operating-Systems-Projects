// Fig. 2.1: fig02_01.c
// A first program in C
#include <stdio.h>
/*
A directive to the C preprocessor. It is processed by the C preprocessor before compilation. It tells 
the preprocessor to include the contents of the standard input/output header (<stdio.h>) in the program. 
The preprocessor will replace the #include statement with the contents of the header file.
*/

// function main begins program execution 
int main( void )
{
   printf( "Welcome to C!\n" );
   // standard output to the screen
} 